jQuery(document).ready(function () {
    $('.burgermenu').on('click', function () {
        // $('.mobile-nav').toggle("");
        // $('.mobile-nav').toggle("slow");
        // $('.mobile-nav').toggle("fast");
        $('.mobile-nav').toggle(700);
    })
});